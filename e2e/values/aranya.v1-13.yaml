image:
  repository: ghcr.io/arhat-dev/aranya
  pullPolicy: Always
  # this tag will be updated before e2e test
  tag: amd64-latest

config:
  aranya:
    leaderElection:
      lock:
        # there is no lease resource in v1.13
        type: endpoints
